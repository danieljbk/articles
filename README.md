# articles — MOVED

> **Moved 2026-08-27.** This content lives on as the `articles/` area of the
> `danieljbk/kwon` system monorepo, history imported
> (`config/claude/decisions/2026-08-27-monorepo.md`). Do not push here. On
> mbp2021, `~/dev/GitHub/danieljbk/articles` already resolves into the
> monorepo. This repo stays unarchived only until the Cloudflare build is
> repointed at kwon; after that it is archived as the frozen pre-move record.
